#!/bin/bash

echo "`date`"
echo "$TRAVIS_OS_NAME"
echo "========================================================================="
#if [ "$TRAVIS_OS_NAME" == "linux" ]; then
#fi
JAR="`ls -1 _build/*.jar`"
jar ft "$JAR"
echo "========================================================================="
java -jar "$JAR" --help
echo "========================================================================="
java -cp "$JAR" oscsend --help
echo "========================================================================="
java -cp "$JAR" oscsend localhost 7890 /hi ifs 1 .2 "bar last"
echo "========================================================================="
echo "`date`"
echo "done"
